import { SxProps } from "@mui/material";

export const flexContainer = {
    display: 'flex',
    justifyContent:'center',
    alignItems: 'center',
    py:5
} as SxProps | undefined

export const flexContainerColumn = {
    display: 'flex',
    justifyContent:'center',
    alignItems: 'center',
    flexDirection: 'column'
} as SxProps | undefined