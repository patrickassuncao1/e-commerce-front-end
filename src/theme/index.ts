import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: '#ffffff'
        },
        secondary:{
            main: '#EF0D0D'
        }
    },
});

export default theme;

